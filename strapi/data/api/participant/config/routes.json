{
  "routes": [
    {
      "method": "GET",
      "path": "/participants",
      "handler": "Participant.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/participants/count",
      "handler": "Participant.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/participants/:id",
      "handler": "Participant.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/participants",
      "handler": "Participant.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/participants/:id",
      "handler": "Participant.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/participants/:id",
      "handler": "Participant.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
